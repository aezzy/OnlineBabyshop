@model Product

<div class="col-sm-4 col-lg-4 col-md-4">
    <div class="thumbnail">
        @*<img src="@Model.ImageUrl" alt="" />*@
        <div class="caption">

            <h3>
                <a asp-controller="Product" asp-action="Details" asp-route-categoryId="@Model.CategoryId">@Model.ProductName</a>
            </h3>

            <p>@Model.ProductDisc</p>

            <h3 class="pull-right">@Model.Price.ToString("c")</h3>
        </div>
        @*<div class="addToCart text-right">
            <p class="button ">
                <a class="btn btn-success" id="cartButton" asp-controller="ShoppingCart" asp-action="AddToShoppingCart" asp-route-drinkId="@Model.DrinkId">
                    Add to cart
                </a>
            </p>
        </div>*@
    </div>
</div>